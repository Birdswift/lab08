name: Docker

on:
  push:
    branches: 
      main 

jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Build
        run: |
          sudo docker build -t myapp1 ./server
          sudo docker run myapp1
           - name: Build
      - name: Build
        run: |
          sudo docker build -t myapp2 ./client
          sudo docker run myapp2
